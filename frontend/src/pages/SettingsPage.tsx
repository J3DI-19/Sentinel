import { useState } from "react";
import { Button, PageHeader, Panel } from "../components/ui/core";
export function SettingsPage(){const [tab,setTab]=useState("Preferences");return <><PageHeader eyebrow="Platform configuration" title="Settings" description="Local interface preferences and placeholders for planned platform configuration."/><div className="settings-layout"><aside className="settings-nav">{["Preferences","Model settings","Alert rules","Email delivery"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}<span>›</span></button>)}</aside><Panel className="settings-panel" title={tab} action={<span className="mock-label">Placeholder settings</span>}><div className="setting-row"><div><b>Use system theme</b><p>Traceveil currently ships with its investigation-optimized dark theme.</p></div><label className="switch"><input type="checkbox" defaultChecked/><i/></label></div><div className="setting-row"><div><b>Compact evidence rows</b><p>Show more evidence records within the same viewport.</p></div><label className="switch"><input type="checkbox"/><i/></label></div><div className="setting-row"><div><b>Reduce motion</b><p>Disable animated graph edges and non-essential transitions.</p></div><label className="switch"><input type="checkbox"/><i/></label></div><div className="setting-field"><label>Default case timezone</label><select><option>Asia/Kolkata (UTC+05:30)</option><option>UTC</option></select></div><div className="settings-save"><span>No persistence is implemented for this milestone.</span><Button variant="primary">Save preferences</Button></div></Panel></div></>}
